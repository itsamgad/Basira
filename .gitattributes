# Force CRLF line endings on Windows-only files so they work natively
# in CMD even when the repository is cloned from a Mac/Linux machine.
*.bat       text eol=crlf
*.cmd       text eol=crlf

# Keep shell scripts as LF on every platform (macOS / Linux).
*.command   text eol=lf
*.sh        text eol=lf
